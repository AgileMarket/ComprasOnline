import { useState } from "react"; //guardar los contactos
import { AddContact } from "../components/contact"; //Adiccionar los contactos

export const Phonebook = {} => {
    const [contacts, setContacts] = useState([]);
};